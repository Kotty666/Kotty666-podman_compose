# Reference

<!-- DO NOT EDIT: This document was generated by Puppet Strings -->

## Table of Contents

### Classes

#### Public Classes

* [`podman_compose`](#podman_compose): Install podman and podman-compose, manage compose projects from Hiera

#### Private Classes

* `podman_compose::install`: Install podman and podman-compose

### Defined types

* [`podman_compose::cron`](#podman_compose--cron): Manage a scheduled podman-compose job via systemd timer
* [`podman_compose::project`](#podman_compose--project): Manage a single podman-compose project
* [`podman_compose::registry`](#podman_compose--registry): Manage a container registry login for a specific user
* [`podman_compose::user`](#podman_compose--user): Manage a system user for rootless podman-compose

### Data types

* [`Podman_compose::Ensure`](#Podman_compose--Ensure): Ensure state for a podman-compose project
* [`Podman_compose::Install_method`](#Podman_compose--Install_method): Install method for podman-compose
* [`Podman_compose::Recreate_strategy`](#Podman_compose--Recreate_strategy): How a project's containers are (re)created when the compose file
or .env changes.
* [`Podman_compose::Registries`](#Podman_compose--Registries): Registry credentials hash structure

## Classes

### <a name="podman_compose"></a>`podman_compose`

Installs podman and podman-compose, then iterates over projects defined
in Hiera to create compose files and systemd services.

#### Examples

##### Basic usage via Hiera

```puppet
include podman_compose
```

##### Hiera data

```puppet
podman_compose::projects:
  myapp:
    user: appuser
    rootless: true
    compose:
      services:
        web:
          image: "nginx:1.27"
          ports:
            - "8080:80"

podman_compose::cron_projects:
  backup:
    user: backup
    rootless: true
    on_calendar: '*-*-* 02:00:00'
    run_service: restic
    compose:
      services:
        restic:
          image: "restic/restic:0.17"
          command: ["backup", "/data"]
```

#### Parameters

The following parameters are available in the `podman_compose` class:

* [`manage_package`](#-podman_compose--manage_package)
* [`podman_package`](#-podman_compose--podman_package)
* [`compose_install_method`](#-podman_compose--compose_install_method)
* [`compose_package`](#-podman_compose--compose_package)
* [`compose_pip_package`](#-podman_compose--compose_pip_package)
* [`compose_ensure`](#-podman_compose--compose_ensure)
* [`compose_binary`](#-podman_compose--compose_binary)
* [`manage_systemd_container`](#-podman_compose--manage_systemd_container)
* [`systemd_container_package`](#-podman_compose--systemd_container_package)
* [`user_compose_dir_name`](#-podman_compose--user_compose_dir_name)
* [`root_compose_dir`](#-podman_compose--root_compose_dir)
* [`projects`](#-podman_compose--projects)
* [`cron_projects`](#-podman_compose--cron_projects)

##### <a name="-podman_compose--manage_package"></a>`manage_package`

Data type: `Boolean`

Whether to manage the podman package installation.

##### <a name="-podman_compose--podman_package"></a>`podman_package`

Data type: `String[1]`

Name of the podman package to install.

##### <a name="-podman_compose--compose_install_method"></a>`compose_install_method`

Data type: `Podman_compose::Install_method`

How to install podman-compose: 'package' (apt/dnf) or 'pip'.

##### <a name="-podman_compose--compose_package"></a>`compose_package`

Data type: `String[1]`

Package name when using install method 'package'.

##### <a name="-podman_compose--compose_pip_package"></a>`compose_pip_package`

Data type: `String[1]`

Pip package name when using install method 'pip'.

##### <a name="-podman_compose--compose_ensure"></a>`compose_ensure`

Data type: `String[1]`

Ensure state for podman-compose package.

##### <a name="-podman_compose--compose_binary"></a>`compose_binary`

Data type: `Stdlib::Absolutepath`

Absolute path to the podman-compose binary.

##### <a name="-podman_compose--manage_systemd_container"></a>`manage_systemd_container`

Data type: `Boolean`

Whether to install the systemd-container package (provides machinectl).
Useful for opening proper user systemd sessions via
`machinectl shell <user>@` with correctly initialized
XDG_RUNTIME_DIR and DBUS_SESSION_BUS_ADDRESS.

##### <a name="-podman_compose--systemd_container_package"></a>`systemd_container_package`

Data type: `String[1]`

Name of the systemd-container package.

##### <a name="-podman_compose--user_compose_dir_name"></a>`user_compose_dir_name`

Data type: `String[1]`

Name of the parent directory under each user's home that holds rootless
compose projects. Default: 'compose' (resulting in /home/<user>/compose/<project>).

##### <a name="-podman_compose--root_compose_dir"></a>`root_compose_dir`

Data type: `Stdlib::Absolutepath`

Absolute base directory for rootful compose projects. Default: /opt/compose.

##### <a name="-podman_compose--projects"></a>`projects`

Data type: `Hash[String[1], Hash]`

Hash of podman_compose::project resources, typically from Hiera lookup.

##### <a name="-podman_compose--cron_projects"></a>`cron_projects`

Data type: `Hash[String[1], Hash]`

Hash of podman_compose::cron resources for scheduled/periodic jobs.

## Defined types

### <a name="podman_compose--cron"></a>`podman_compose::cron`

Renders a compose file, creates a oneshot systemd service and a
timer unit. The timer fires on the configured schedule, starts the
compose stack, waits for completion, then tears it down.

Two run modes are supported:
- **full stack** (default): `podman-compose up --abort-on-container-exit`
  followed by `podman-compose down`. Useful when the job needs
  sidecars (database, queue, etc.).
- **single service** (`run_service`): `podman-compose run --rm <svc>`.
  Lighter weight, ideal when only one container does the work.

#### Examples

##### Daily backup at 2 AM

```puppet
podman_compose::cron_projects:
  backup:
    user: backup
    rootless: true
    on_calendar: '*-*-* 02:00:00'
    randomized_delay_sec: '15min'
    run_service: restic
    compose:
      services:
        restic:
          image: "restic/restic:0.17"
          volumes:
            - "/data:/data:ro"
            - "./cache:/var/cache/restic"
          env_file:
            - .env
          command: ["backup", "/data", "--verbose"]
```

##### Hourly cleanup with sidecars

```puppet
podman_compose::cron_projects:
  cleanup:
    rootless: false
    on_calendar: 'hourly'
    persistent: false
    compose:
      services:
        worker:
          image: "myorg/cleanup:latest"
          depends_on:
            - redis
        redis:
          image: "redis:7-alpine"
```

#### Parameters

The following parameters are available in the `podman_compose::cron` defined type:

* [`compose`](#-podman_compose--cron--compose)
* [`on_calendar`](#-podman_compose--cron--on_calendar)
* [`ensure`](#-podman_compose--cron--ensure)
* [`rootless`](#-podman_compose--cron--rootless)
* [`user`](#-podman_compose--cron--user)
* [`group`](#-podman_compose--cron--group)
* [`manage_user`](#-podman_compose--cron--manage_user)
* [`compose_dir`](#-podman_compose--cron--compose_dir)
* [`env_vars`](#-podman_compose--cron--env_vars)
* [`env_secrets`](#-podman_compose--cron--env_secrets)
* [`run_service`](#-podman_compose--cron--run_service)
* [`pull_before_run`](#-podman_compose--cron--pull_before_run)
* [`on_boot_sec`](#-podman_compose--cron--on_boot_sec)
* [`persistent`](#-podman_compose--cron--persistent)
* [`randomized_delay_sec`](#-podman_compose--cron--randomized_delay_sec)
* [`accuracy_sec`](#-podman_compose--cron--accuracy_sec)
* [`compose_file_name`](#-podman_compose--cron--compose_file_name)
* [`service_timeout`](#-podman_compose--cron--service_timeout)
* [`extra_systemd_config`](#-podman_compose--cron--extra_systemd_config)
* [`registries`](#-podman_compose--cron--registries)

##### <a name="-podman_compose--cron--compose"></a>`compose`

Data type: `Hash`

Compose file content as a Hash. Must contain 'services'.

##### <a name="-podman_compose--cron--on_calendar"></a>`on_calendar`

Data type: `String[1]`

Systemd OnCalendar expression (see `man systemd.time`).
Examples: 'daily', 'weekly', '*-*-* 02:00:00', 'Mon *-*-* 06:30:00'

##### <a name="-podman_compose--cron--ensure"></a>`ensure`

Data type: `Enum['present', 'absent']`

'present' or 'absent'.

Default value: `'present'`

##### <a name="-podman_compose--cron--rootless"></a>`rootless`

Data type: `Boolean`

Run as unprivileged user.

Default value: `true`

##### <a name="-podman_compose--cron--user"></a>`user`

Data type: `Optional[String[1]]`

System user (required when rootless).

Default value: `undef`

##### <a name="-podman_compose--cron--group"></a>`group`

Data type: `Optional[String[1]]`

Primary group. Defaults to $user.

Default value: `undef`

##### <a name="-podman_compose--cron--manage_user"></a>`manage_user`

Data type: `Boolean`

Whether to manage the system user resource.

Default value: `true`

##### <a name="-podman_compose--cron--compose_dir"></a>`compose_dir`

Data type: `Optional[Stdlib::Absolutepath]`

Project directory. Auto-derived if unset.

Default value: `undef`

##### <a name="-podman_compose--cron--env_vars"></a>`env_vars`

Data type: `Hash[String[1], String]`

KEY=VALUE pairs for .env file.

Default value: `{}`

##### <a name="-podman_compose--cron--env_secrets"></a>`env_secrets`

Data type: `Hash[String[1], Sensitive[String]]`

Sensitive KEY=VALUE pairs merged into .env (eyaml-friendly).

Default value: `{}`

##### <a name="-podman_compose--cron--run_service"></a>`run_service`

Data type: `Optional[String[1]]`

If set, use `podman-compose run --rm <run_service>` instead of
bringing up the full stack. Name must match a service in compose.

Default value: `undef`

##### <a name="-podman_compose--cron--pull_before_run"></a>`pull_before_run`

Data type: `Boolean`

Pull images before each run.

Default value: `true`

##### <a name="-podman_compose--cron--on_boot_sec"></a>`on_boot_sec`

Data type: `Optional[String[1]]`

Optional OnBootSec for timer (e.g. '5min'). Runs once after boot.

Default value: `undef`

##### <a name="-podman_compose--cron--persistent"></a>`persistent`

Data type: `Boolean`

Catch up missed runs if the machine was off. Default: true.

Default value: `true`

##### <a name="-podman_compose--cron--randomized_delay_sec"></a>`randomized_delay_sec`

Data type: `Optional[String[1]]`

Jitter window to spread load (e.g. '15min').

Default value: `undef`

##### <a name="-podman_compose--cron--accuracy_sec"></a>`accuracy_sec`

Data type: `Optional[String[1]]`

Timer coalescing accuracy (e.g. '1s' for precise, '1min' default).

Default value: `undef`

##### <a name="-podman_compose--cron--compose_file_name"></a>`compose_file_name`

Data type: `String[1]`

Name of the compose file. Default: compose.yml (the modern Compose Spec
name). Set to 'docker-compose.yml' for the legacy filename.

Default value: `'compose.yml'`

##### <a name="-podman_compose--cron--service_timeout"></a>`service_timeout`

Data type: `Integer[60]`

TimeoutStartSec for the oneshot service.

Default value: `600`

##### <a name="-podman_compose--cron--extra_systemd_config"></a>`extra_systemd_config`

Data type: `Hash[String, String]`

Additional [Service] directives.

Default value: `{}`

##### <a name="-podman_compose--cron--registries"></a>`registries`

Data type: `Podman_compose::Registries`

Hash of registry server => {username, password} for `podman login`.
Same semantics as podman_compose::project's `registries`: credentials
are piped via stdin and tracked via SHA256 sentinel for change
detection.

Default value: `{}`

### <a name="podman_compose--project"></a>`podman_compose::project`

Creates the compose directory, renders the compose file from a Hash,
optionally manages an .env file, and sets up a systemd service to
keep the project running.

#### Examples

##### Hiera definition

```puppet
podman_compose::projects:
  traefik:
    rootless: false
    compose:
      services:
        traefik:
          image: "traefik:v3.1"
          ports:
            - "80:80"
            - "443:443"
          volumes:
            - "/var/run/podman/podman.sock:/var/run/docker.sock:ro"
            - "./config:/etc/traefik:ro"
          restart: unless-stopped
```

#### Parameters

The following parameters are available in the `podman_compose::project` defined type:

* [`compose`](#-podman_compose--project--compose)
* [`ensure`](#-podman_compose--project--ensure)
* [`rootless`](#-podman_compose--project--rootless)
* [`user`](#-podman_compose--project--user)
* [`group`](#-podman_compose--project--group)
* [`manage_user`](#-podman_compose--project--manage_user)
* [`compose_dir`](#-podman_compose--project--compose_dir)
* [`env_vars`](#-podman_compose--project--env_vars)
* [`env_secrets`](#-podman_compose--project--env_secrets)
* [`pull_on_start`](#-podman_compose--project--pull_on_start)
* [`compose_file_name`](#-podman_compose--project--compose_file_name)
* [`service_timeout`](#-podman_compose--project--service_timeout)
* [`extra_systemd_config`](#-podman_compose--project--extra_systemd_config)
* [`registries`](#-podman_compose--project--registries)
* [`verify_running_image`](#-podman_compose--project--verify_running_image)
* [`recreate_strategy`](#-podman_compose--project--recreate_strategy)

##### <a name="-podman_compose--project--compose"></a>`compose`

Data type: `Hash`

The compose file content as a Puppet Hash. Gets rendered to YAML.
Must contain at least a 'services' key.

##### <a name="-podman_compose--project--ensure"></a>`ensure`

Data type: `Podman_compose::Ensure`

Desired state: 'running', 'stopped', or 'absent'.

Default value: `'running'`

##### <a name="-podman_compose--project--rootless"></a>`rootless`

Data type: `Boolean`

Run containers as unprivileged user (true) or as root (false).

Default value: `true`

##### <a name="-podman_compose--project--user"></a>`user`

Data type: `Optional[String[1]]`

System user owning the project. Required when rootless is true.

Default value: `undef`

##### <a name="-podman_compose--project--group"></a>`group`

Data type: `Optional[String[1]]`

Primary group for the user. Defaults to $user.

Default value: `undef`

##### <a name="-podman_compose--project--manage_user"></a>`manage_user`

Data type: `Boolean`

Whether to manage the system user resource.

Default value: `true`

##### <a name="-podman_compose--project--compose_dir"></a>`compose_dir`

Data type: `Optional[Stdlib::Absolutepath]`

Absolute path to the project directory. Auto-derived if unset.

Default value: `undef`

##### <a name="-podman_compose--project--env_vars"></a>`env_vars`

Data type: `Hash[String[1], String]`

Optional Hash of KEY=VALUE pairs written to an .env file.

Default value: `{}`

##### <a name="-podman_compose--project--env_secrets"></a>`env_secrets`

Data type: `Hash[String[1], Sensitive[String]]`

Optional Hash of sensitive KEY=VALUE pairs merged into .env.
Values should come from Hiera eyaml or similar.

Default value: `{}`

##### <a name="-podman_compose--project--pull_on_start"></a>`pull_on_start`

Data type: `Boolean`

Pull images before (re)starting the service.

Default value: `true`

##### <a name="-podman_compose--project--compose_file_name"></a>`compose_file_name`

Data type: `String[1]`

Name of the compose file. Default: compose.yml (the modern Compose Spec
name). Set to 'docker-compose.yml' for the legacy filename.

Default value: `'compose.yml'`

##### <a name="-podman_compose--project--service_timeout"></a>`service_timeout`

Data type: `Integer[60]`

Systemd TimeoutStartSec for pull + start.

Default value: `300`

##### <a name="-podman_compose--project--extra_systemd_config"></a>`extra_systemd_config`

Data type: `Hash[String, String]`

Additional systemd unit directives merged into [Service].

Default value: `{}`

##### <a name="-podman_compose--project--registries"></a>`registries`

Data type: `Podman_compose::Registries`

Hash of registry server => {username, password} for `podman login`.
Credentials are piped via stdin. Change detection via SHA256 sentinel.

Default value: `{}`

##### <a name="-podman_compose--project--verify_running_image"></a>`verify_running_image`

Data type: `Boolean`

On every Puppet run, compare each service's running container image
digest with the desired image (after a quiet `pull`). On drift, run
`podman-compose up -d --remove-orphans` so only affected services are
recreated. Detects manual changes, registry digest updates behind a
stable tag (e.g. ':latest'), and missing containers.

Default value: `true`

##### <a name="-podman_compose--project--recreate_strategy"></a>`recreate_strategy`

Data type: `Podman_compose::Recreate_strategy`

How containers are (re)created when the compose file or .env change.
Defaults to 'force-recreate' so env changes are always applied cleanly
(a plain `up -d` may leave running containers with stale `env_file:`
values). Use 'down-up' when you change network definitions (subnet,
driver, options), since `up` alone never re-creates an existing network
— at the cost of a brief project downtime. Use 'rolling' for the old,
least-disruptive `up -d` behaviour.

Default value: `'force-recreate'`

### <a name="podman_compose--registry"></a>`podman_compose::registry`

Runs `podman login` with credentials piped via stdin (no password
in the process list). A SHA256 sentinel file tracks credential
changes — if username or password change in Hiera, the login is
re-executed on the next Puppet run.

Can be used standalone or auto-created via the `registries`
parameter on podman_compose::project / podman_compose::cron.

#### Examples

##### Standalone usage

```puppet
podman_compose::registry { 'appuser@registry.example.com':
  server   => 'registry.example.com',
  username => 'deploy',
  password => Sensitive('s3cret'),
  user     => 'appuser',
  rootless => true,
}
```

##### Via Hiera (auto-created by project/cron)

```puppet
podman_compose::projects:
  myapp:
    user: appuser
    registries:
      registry.example.com:
        username: deploy
        password: ENC[PKCS7,...]
```

#### Parameters

The following parameters are available in the `podman_compose::registry` defined type:

* [`server`](#-podman_compose--registry--server)
* [`username`](#-podman_compose--registry--username)
* [`password`](#-podman_compose--registry--password)
* [`user`](#-podman_compose--registry--user)
* [`rootless`](#-podman_compose--registry--rootless)
* [`ensure`](#-podman_compose--registry--ensure)

##### <a name="-podman_compose--registry--server"></a>`server`

Data type: `String[1]`

Registry FQDN (e.g. 'registry.example.com', 'ghcr.io').

##### <a name="-podman_compose--registry--username"></a>`username`

Data type: `String[1]`

Registry username or token name.

##### <a name="-podman_compose--registry--password"></a>`password`

Data type: `Sensitive[String[1]]`

Registry password or access token (Sensitive).

##### <a name="-podman_compose--registry--user"></a>`user`

Data type: `String[1]`

System user to authenticate. Determines auth.json location.

Default value: `'root'`

##### <a name="-podman_compose--registry--rootless"></a>`rootless`

Data type: `Boolean`

Whether the user runs rootless podman.

Default value: `true`

##### <a name="-podman_compose--registry--ensure"></a>`ensure`

Data type: `Enum['present', 'absent']`

'present' to log in, 'absent' to log out and remove sentinel.

Default value: `'present'`

### <a name="podman_compose--user"></a>`podman_compose::user`

Creates the user, enables loginctl linger (so user services
persist after logout), and ensures required directories exist.

#### Parameters

The following parameters are available in the `podman_compose::user` defined type:

* [`user`](#-podman_compose--user--user)
* [`home`](#-podman_compose--user--home)
* [`shell`](#-podman_compose--user--shell)
* [`system_user`](#-podman_compose--user--system_user)
* [`manage_home`](#-podman_compose--user--manage_home)

##### <a name="-podman_compose--user--user"></a>`user`

Data type: `String[1]`

Username to manage. Defaults to the resource title.

Default value: `$name`

##### <a name="-podman_compose--user--home"></a>`home`

Data type: `Stdlib::Absolutepath`

Home directory path.

Default value: `"/home/${name}"`

##### <a name="-podman_compose--user--shell"></a>`shell`

Data type: `Stdlib::Absolutepath`

Login shell.

Default value: `'/bin/bash'`

##### <a name="-podman_compose--user--system_user"></a>`system_user`

Data type: `Boolean`

Create as system user.

Default value: `true`

##### <a name="-podman_compose--user--manage_home"></a>`manage_home`

Data type: `Boolean`

Let Puppet manage the home directory.

Default value: `true`

## Data types

### <a name="Podman_compose--Ensure"></a>`Podman_compose::Ensure`

Ensure state for a podman-compose project

Alias of `Enum['running', 'stopped', 'absent']`

### <a name="Podman_compose--Install_method"></a>`Podman_compose::Install_method`

Install method for podman-compose

Alias of `Enum['package', 'pip']`

### <a name="Podman_compose--Recreate_strategy"></a>`Podman_compose::Recreate_strategy`

* `rolling`        - `up -d --remove-orphans`. Least disruptive; relies on
                     podman-compose's own change detection. May NOT pick up
                     `.env` content changes delivered via `env_file:` and
                     will NOT re-create existing networks.
* `force-recreate` - `up -d --force-recreate --remove-orphans`. Re-creates
                     every container so new env values and changed service
                     network membership are applied cleanly. Does NOT
                     re-create existing network definitions (subnet/driver).
* `down-up`        - `down` then `up -d --remove-orphans`. Tears the project
                     down (including its networks) and brings it back up, so
                     network topology changes are applied too. Causes a brief
                     downtime of the whole project.

Alias of `Enum['rolling', 'force-recreate', 'down-up']`

### <a name="Podman_compose--Registries"></a>`Podman_compose::Registries`

Registry credentials hash structure

#### Examples

##### 

```puppet
{
  'registry.example.com' => { 'username' => 'deploy', 'password' => Sensitive('secret') },
  'ghcr.io'              => { 'username' => 'bot',    'password' => Sensitive('token') },
}
```

Alias of

```puppet
Hash[String[1], Struct[{
    username => String[1],
    password => Sensitive[String[1]],
  }]]
```

